import { describe,it,expect } from 'vitest'; import { detectIntent } from './intentService';
describe('intent engine',()=>it('detects football discovery from search and views',()=>{const now=new Date().toISOString(); const e:any[]=[{eventType:'SEARCH',metadata:{query:'football tonight'},createdAt:now},{eventType:'CONTENT_VIEWED',metadata:{category:'Football'},createdAt:now},{eventType:'CONTENT_VIEWED',metadata:{category:'Football'},createdAt:now},{eventType:'CONTENT_VIEWED',metadata:{category:'Football'},createdAt:now}]; expect(detectIntent(e).intent).toBe('FOOTBALL_DISCOVERY');}));
