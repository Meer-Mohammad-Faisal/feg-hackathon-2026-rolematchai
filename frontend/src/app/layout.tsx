import './globals.css';
export const metadata={title:'SessionIQ — Session Intelligence',description:'A demo of better session quality through transparent, contextual relevance.'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
