// Demo seed contract: the backend's deterministic catalog is the zero-setup seed.
// A production adapter would insert the same fictional records into PostgreSQL.
console.log('SessionIQ demo seed is available in backend/src/data.ts (36 fictional records).');
